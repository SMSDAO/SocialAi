# =============================================================================
# SocialAi Environment Configuration
# =============================================================================
# Copy this file to .env and configure with your actual values.
# See docs/INSTALLATION.md for detailed setup instructions.
#
# SECURITY WARNING: Never commit .env to version control!
#
# DEPLOYMENT NOTES:
# - VERCEL (Frontend): Only set PUBLIC_API_URL in Vercel project settings
# - BACKEND (Railway/Render): Set all other secrets in backend environment
# - Never add backend secrets (DATABASE_URL, JWT_SECRET, etc.) to Vercel
# - All placeholder values use PLACEHOLDER_ prefix and must be replaced
# =============================================================================

# -----------------------------------------------------------------------------
# Node Environment
# -----------------------------------------------------------------------------
# Options: development, production, test
NODE_ENV=development

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
# Local example: postgresql://postgres:postgres@localhost:5432/socialai
# BACKEND ONLY: Set in Railway/Render/backend environment, NOT in Vercel
DATABASE_URL=postgresql://PLACEHOLDER_USERNAME:PLACEHOLDER_PASSWORD@PLACEHOLDER_HOST:5432/PLACEHOLDER_DATABASE

# Connection pool settings (optional)
# DB_POOL_MIN=2
# DB_POOL_MAX=10

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
# Backend API port
PORT=3000

# API base URL
# Backend uses API_URL; Astro frontend uses PUBLIC_API_URL
# Astro requires PUBLIC_ prefix for client-side environment variables
API_URL=http://localhost:3000
# VERCEL: Set PUBLIC_API_URL to your backend URL (e.g., https://api.yourdomain.com)
PUBLIC_API_URL=http://localhost:3000

# CORS origin (comma-separated for multiple origins)
# Development: http://localhost:4321
# Production: https://yourdomain.com
CORS_ORIGIN=http://localhost:4321

# -----------------------------------------------------------------------------
# Security Configuration
# -----------------------------------------------------------------------------
# JWT secret for authentication (generate a long random string)
# Generate with: openssl rand -base64 64
# BACKEND ONLY: Set in Railway/Render/backend environment, NOT in Vercel
JWT_SECRET=PLACEHOLDER_GENERATE_RANDOM_STRING_HERE

# Session secret (generate a long random string)
# Generate with: openssl rand -base64 64
# BACKEND ONLY: Set in Railway/Render/backend environment, NOT in Vercel
SESSION_SECRET=PLACEHOLDER_GENERATE_RANDOM_STRING_HERE

# -----------------------------------------------------------------------------
# Worker Configuration
# -----------------------------------------------------------------------------
# Farcaster Hub URL
FARCASTER_HUB_URL=https://hub.farcaster.xyz

# Reddit API URL (public endpoints)
REDDIT_API_URL=https://www.reddit.com

# Reddit API credentials (optional, for authenticated requests)
# REDDIT_CLIENT_ID=your_reddit_client_id
# REDDIT_CLIENT_SECRET=your_reddit_client_secret

# -----------------------------------------------------------------------------
# Blockchain RPC URLs
# -----------------------------------------------------------------------------
# Ethereum RPC endpoint
# Options: Public (LlamaRPC), Infura, Alchemy, or your own node
ETH_RPC_URL=https://eth.llamarpc.com
# ETH_RPC_URL=https://mainnet.infura.io/v3/PLACEHOLDER_INFURA_KEY
# ETH_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/PLACEHOLDER_ALCHEMY_KEY

# BASE blockchain RPC endpoint
BASE_RPC_URL=https://mainnet.base.org

# Solana RPC endpoint
# Options: Public (mainnet-beta), Quicknode, Helius, or your own node
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
# SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# -----------------------------------------------------------------------------
# AI Configuration (Optional)
# -----------------------------------------------------------------------------
# OpenAI API key for AI features (summaries, recommendations, embeddings)
# Get your API key from: https://platform.openai.com/api-keys
# BACKEND ONLY: Set in Railway/Render/backend environment, NOT in Vercel
OPENAI_API_KEY=PLACEHOLDER_YOUR_OPENAI_API_KEY_HERE

# OpenAI model to use (optional, defaults to gpt-3.5-turbo)
# OPENAI_MODEL=gpt-3.5-turbo
# OPENAI_MODEL=gpt-4

# OpenAI embedding model (optional, defaults to text-embedding-ada-002)
# OPENAI_EMBEDDING_MODEL=text-embedding-ada-002

# -----------------------------------------------------------------------------
# Feature Flags (Default Values)
# -----------------------------------------------------------------------------
# These can be overridden via the database settings table or admin console
# Set to 'true' or 'false'

# Enable Farcaster data synchronization
ENABLE_FARCASTER_SYNC=true

# Enable Reddit data synchronization
ENABLE_REDDIT_SYNC=false

# Enable AI-powered content summaries
ENABLE_AI_SUMMARIES=true

# Enable AI-powered user recommendations
ENABLE_AI_RECOMMENDATIONS=true

# Enable public profile pages
ENABLE_PUBLIC_PROFILES=true

# Enable identity claim system
ENABLE_IDENTITY_CLAIMS=true

# Enable search functionality
ENABLE_SEARCH=true

# -----------------------------------------------------------------------------
# Rate Limiting Configuration (Optional)
# -----------------------------------------------------------------------------
# Request rate limit per IP (requests per minute)
# RATE_LIMIT_WINDOW_MS=60000
# RATE_LIMIT_MAX_REQUESTS=100

# -----------------------------------------------------------------------------
# Logging Configuration (Optional)
# -----------------------------------------------------------------------------
# Log level: error, warn, info, debug, trace
# LOG_LEVEL=info

# Log to file (optional)
# LOG_FILE=./logs/socialai.log

# -----------------------------------------------------------------------------
# Email Configuration (Optional, for future features)
# -----------------------------------------------------------------------------
# SMTP configuration for sending emails
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password
# SMTP_FROM=noreply@yourdomain.com

# -----------------------------------------------------------------------------
# CDN Configuration (Optional, for production)
# -----------------------------------------------------------------------------
# CDN URL for static assets
# CDN_URL=https://cdn.yourdomain.com

# -----------------------------------------------------------------------------
# Analytics Configuration (Optional)
# -----------------------------------------------------------------------------
# Google Analytics tracking ID
# GA_TRACKING_ID=UA-XXXXXXXXX-X

# Sentry DSN for error tracking
# SENTRY_DSN=https://your_sentry_dsn

# -----------------------------------------------------------------------------
# Additional Configuration
# -----------------------------------------------------------------------------
# Public app URL (for generating links, etc.)
# PUBLIC_URL=http://localhost:4321

# Admin app URL
# ADMIN_URL=http://localhost:4200

# =============================================================================
# End of Configuration
# =============================================================================
# For more information, see:
# - Installation Guide: docs/INSTALLATION.md
# - Environment Configuration: docs/INSTALLATION.md#environment-configuration
# - Deployment Guide: docs/DEPLOYMENT.md
# =============================================================================
